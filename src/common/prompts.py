"""
Prompt templates and formatting.

YOUR JOB: implement.

Key decisions to make and document in notes/project1_design.md:
  - What is the system prompt for Project 1?
  - What is the exact output schema (JSON keys, field order)?
  - How do you apply the chat template for Qwen2.5-Instruct?
    (Qwen uses ChatML format — understand what tokenizer.apply_chat_template does)

Suggested functions:
  format_sft_prompt(example: dict) -> str
  format_pref_prompt(example: dict) -> str
  format_rl_prompt(example: dict) -> str
"""
