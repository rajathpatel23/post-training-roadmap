"""
Misc utilities.

YOUR JOB: implement as needed.

Suggested starters:
  set_seed(seed: int) -> None
  get_device() -> str
  count_parameters(model) -> dict   # total, trainable, trainable %
  format_metrics(metrics: dict) -> str   # pretty-print for terminal
"""
