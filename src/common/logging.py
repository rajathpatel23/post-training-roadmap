"""
W&B logging helpers.

YOUR JOB: implement.

Suggested functions:
  init_run(config: dict) -> wandb.Run
  log_samples(run, samples: list[dict], step: int, table_name: str) -> None
    # logs a W&B Table with prompt / base_output / trained_output columns
  log_metrics(run, metrics: dict, step: int) -> None
  finish_run(run) -> None
"""
