"""
Exact match and format validity metrics — used across all three projects.

YOUR JOB: implement.

Suggested functions:
  format_validity_rate(generations: list[str]) -> float
  exact_field_presence_rate(generations: list[str], required_fields: list[str]) -> float
  task_success_rate(generations: list[str], ground_truths: list[str], verifier_fn) -> float
  avg_response_length(generations: list[str]) -> float
"""
