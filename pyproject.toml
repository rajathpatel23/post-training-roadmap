[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "post-training-roadmap"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "datasets",
    "trl",
    "peft",
    "accelerate",
    "wandb",
    "evaluate",
    "scikit-learn",
    "pandas",
    "jupyter",
    "pyyaml",
    "bitsandbytes",
    "matplotlib>=3.11.0",
    "anthropic",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
